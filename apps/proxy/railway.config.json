{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "bun run build --filter=proxy",
      "watchPatterns": [
        "apps/proxy/**/*",
        "packages/auth/**/*",
        "packages/db/**/*",
        ".env*",
        "turbo.json",
        "package.json"
      ]
      },
    "deploy": {
      "startCommand": "cd apps/proxy && bun run dist/index.mjs",
      "healthcheckPath": "/",
      "healthcheckTimeout": 100,
      "restartPolicyType": "never"
      }
  }