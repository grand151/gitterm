{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "./apps/proxy/Dockerfile.managed",
    "watchPatterns": ["apps/proxy/**/*", "turbo.json", "package.json", "bun.lock"]
  }
}
