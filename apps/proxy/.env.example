# Internal API key for forward_auth to server
# Must match INTERNAL_API_KEY in server's .env
# Defaults to 'dev-internal-key' if not set in Caddyfile.local
INTERNAL_API_KEY=dev-internal-key
SERVER_URL=
TUNNEL_PROXY_URL=
ROUTING_MODE=path