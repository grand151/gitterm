{
  "name": "tunnel-proxy",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "tsdown && cp -r src/errors dist/",
    "start": "bun run dist/index.mjs",
    "check-types": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gitterm/api": "workspace:*",
    "@gitterm/env": "workspace:*",
    "@gitterm/redis": "workspace:*",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "jsonwebtoken": "^9.0.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@gitterm/config": "workspace:*",
    "@types/bun": "^1.2.6",
    "@types/jsonwebtoken": "^9.0.7",
    "tsdown": "^0.16.5",
    "typescript": "catalog:"
  }
}
