# URL to tunnel-proxy websocket endpoint
# Example: ws://localhost:9000/tunnel/connect
TUNNEL_PROXY_WS_URL=

# Tunnel JWT minted by API (`trpc.tunnel.mintToken`)
TUNNEL_TOKEN=

# Primary local port to expose for the tunnel
TUNNEL_PRIMARY_PORT=3000

# Base URL for the local service to proxy to.
# Example: http://localhost:3000
TARGET_BASE_URL=http://localhost:3000
